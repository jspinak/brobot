// settings.gradle for the brobot project

rootProject.name = 'brobot'
include 'library', 'library-features', 'runner'
// 'library-test' module disabled - tests are outdated and reference non-existent classes
