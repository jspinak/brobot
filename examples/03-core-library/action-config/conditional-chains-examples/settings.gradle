rootProject.name = 'conditional-chains-examples'

// Use local Brobot library
includeBuild('../../../../') {
    dependencySubstitution {
        substitute module('io.github.jspinak:brobot') using project(':library')
    }
}