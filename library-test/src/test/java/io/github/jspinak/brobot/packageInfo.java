/**
 * This package contains integration tests for the Brobot library.
 *
 * Tests in this package:
 * - Require Spring Boot application context
 * - Test multiple components working together
 * - Verify library behavior in a real application
 * - May perform actual GUI operations
 *
 * For unit tests that don't require Spring Boot,
 * use the test directory in the library module instead.
 */
package io.github.jspinak.brobot;
